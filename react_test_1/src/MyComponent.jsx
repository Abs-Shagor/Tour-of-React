function MyComponent() {
    return (
        <div>
            <h2>Creating Component in a separate file</h2>
            <p>Note: We have to export the component from the file and should import in the main jsx file(App.jsx) </p>
        </div>
    )
}
export default MyComponent

